import 'package:flutter/material.dart';

class PasswordRicovery extends StatelessWidget {
  const PasswordRicovery({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('بازیابی رمز عبور'),
      ),
    );
  }
}
